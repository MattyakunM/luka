app.jsのみ置き換えてください。server.js / index.html / style.css / luka.json は変更しません。

機能:
- 起動時はホーム画面
- そらだけ管理者
- 個人アカウント作成
- 通常ユーザーはブラウザ/端末プロファイルにつき1アカウント
- 管理者はこの制限の対象外
- 管理画面から通常アカウント作成・切替

注意: V4はlocalStorage版なので、ブラウザデータ削除・別ブラウザ等では端末制限を回避できます。真の端末単位制限はサーバー＋DB版で実装します。
